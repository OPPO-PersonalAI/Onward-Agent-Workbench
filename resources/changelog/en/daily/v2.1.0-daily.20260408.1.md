# Onward Daily Build v2.1.0-daily.20260408.1

Changes since `v2.1.0-daily.20260406.1`.

## New Features
- Add telemetry with Azure Application Insights integration: consent dialog, settings toggle, daily aggregation, and local event logging
- Unify subpage navigation shell: shared SubpageSwitcher for Diff, Editor, and History with centralized lifecycle management
- Show task identity label in fullscreen Diff, Editor, and History headers
- Improve prompt history with task filter, color filter, task pills, and copy prompt support
- Add selection indicator grid in PromptSender aligned with terminal task positions
- Improve markdown preview outline navigation with scroll memory, code wrapping toggle, and effective target fallback
- Add GitHub-backed feedback workflow with sidebar UI, rating, history, and persistence

## Bug Fixes
- Eliminate Git Diff scroll flash with render-then-reveal pattern and background diff cache pre-computation
- Reset terminal selection after prompt send action
- Fix outline panel not updating during live editing
- Fix stale image cache in Markdown preview with auto-refresh and manual refresh button
- Fix terminal PATH resolution and UI state restoration on upgrade
- Fix state persistence across upgrades and add window state restoration
- Unify project editor file memory persistence and sidebar scroll state

## Improvements
- Unify dropdown styling across shared UI components
- Refine settings select spacing and notice generation
- Upgrade runtime and toolchain dependencies for security fixes
- Redesign sidebar icons and unify ChangeLog overlay
